# Состояние проекта

Живой документ. **Обновлять при каждой завершённой фиче** — иначе он быстро станет
таким же недостоверным, как моковые данные, которые описывает.

Дата последней ревизии: 2026-08-14 (этап 1.1 — вертикальный срез услуг).

---

## Объём v1

**Каталог услуг и доска заданий без внутреннего взаимодействия.** Связь между
пользователями — только через раскрытие контактных данных.

Вне v1: отклики · отзывы · рейтинги · чат · заказы · платежи.
Модель — в [DATA-MODEL.md](DATA-MODEL.md), порядок работ — в [ROADMAP.md](ROADMAP.md).

---

## Сводка

Фундамент заложен, первая фича работает на реальных данных. Задания, профиль,
избранное и админка пока на моках.

| | Состояние |
|---|---|
| Аутентификация | Работает |
| Схема БД | Соответствует модели v1, миграция применена |
| Справочники | Города и категории из БД, зафиксированы сидом |
| **Услуги** | **Работают сквозь: создание, каталог, карточка, правка, видимость** |
| Задания, профиль, избранное, админка | Моки |
| Server Actions | 4 (услуги ×3, раскрытие контактов) |
| Тесты | Нет |
| CI | 4 шага, все зелёные |

**Проверено вручную:** создание услуги через форму. При первом прогоне всплыла ошибка
валидации чекбокса — см. ниже, раздел про этап 1.1.

---

## По фичам

Легенда: ✅ работает · 🟡 UI есть, данные моковые · ⬜ не начато · ⛔ вне v1

| Фича | UI | Схема БД | Queries | Actions | Итого |
|---|---|---|---|---|---|
| Регистрация / вход | ✅ | ✅ | ✅ | ✅ | **✅ Работает** |
| Верификация email | ✅ | ✅ | ✅ | ✅ | **✅ Работает** |
| Сброс пароля | ✅ | ✅ | ✅ | ✅ | **✅ Работает** |
| Google OAuth | ✅ | ✅ | ✅ | ✅ | **✅ Работает** |
| Сессии и middleware | — | ✅ | ✅ | ✅ | **✅ Работает** |
| Роль admin, доступ в админку | ✅ | ✅ | ✅ | ✅ | **✅ Работает** |
| Города и категории | ✅ | ✅ | ✅ | — | **✅ Работает** |
| Услуги: создание и правка | ✅ | ✅ | ✅ | ✅ | **✅ Работает** |
| Услуги: каталог и карточка | ✅ | ✅ | ✅ | — | **✅ Работает** |
| Услуги: «Мои услуги» | ✅ | ✅ | ✅ | ✅ | **✅ Работает** |
| Раскрытие контактов | ✅ | ✅ | ✅ | ✅ | 🟡 Механизм готов, заполнять негде |
| Задания | ✅ | ✅ | ⬜ | ⬜ | 🟡 Моки |
| Профиль пользователя | ✅ | ✅ | 🟡 частично | ⬜ | 🟡 Моки |
| Избранное | 🟡 только услуги | ✅ | ⬜ | ⬜ | 🟡 `useState` |
| Загрузка изображений | 🟡 превью с пометкой «Скоро» | ✅ | ⬜ | ⬜ | ⬜ Не начато |
| Админка: услуги | ✅ | ✅ | ⬜ | ⬜ | 🟡 Моки |
| Админка: задания | ✅ | ✅ | ⬜ | ⬜ | 🟡 Моки |
| Админка: пользователи | ✅ | ✅ | ⬜ | ⬜ | 🟡 Моки |
| Фильтры каталога | ✅ | ✅ | ⬜ | ⬜ | 🟡 `useState` без эффекта |
| Пагинация | ⬜ убрана | — | ⬜ | ⬜ | ⬜ Этап 3 |
| Отклики | ✅ вёрстка есть | — | — | — | **⛔ Вне v1** |
| Отзывы и рейтинги | ✅ вёрстка есть | — | — | — | **⛔ Вне v1** |

Вёрстка откликов и отзывов существует и подлежит удалению на этапе 1 — список в
[DATA-MODEL.md](DATA-MODEL.md), раздел «Последствия для UI».

---

## Файлы с моковыми данными

Список для отслеживания. Удалять строку по мере замены на реальные данные.

**Формы с `TODO: заменить на Server Action`:**
- `features/tasks/components/CreateTaskForm.tsx`
- `features/tasks/components/RespondButton.tsx` — уйдёт целиком, откликов в v1 нет
- `features/profiles/components/ContactSettingsForm.tsx`

**Страницы с захардкоженными массивами:**
- `app/(main)/tasks/page.tsx` · `tasks/[id]/page.tsx` · `dashboard/tasks/` и `[id]/edit`
- `app/(main)/profiles/[username]/page.tsx`
- `app/(main)/dashboard/` — `favorites`, `responses`
- `app/admin/` — `listings`, `tasks`, `users`

**Состояние без персистентности:**
- `components/shared/FavoriteButton.tsx` — `useState`, сбрасывается при навигации
- `app/admin/users/page.tsx` — блокировка живёт только в памяти вкладки
- `features/services/components/CategorySidebar.tsx` и `TasksSidebar.tsx` — фильтры
  меняют локальное состояние и ни на что не влияют

---

## Архитектурные решения — статус внедрения

| Решение | Статус |
|---|---|
| Модель данных v1 утверждена | ✅ [DATA-MODEL.md](DATA-MODEL.md) |
| `schema.ts` под модель v1 | ✅ Переписан, `tsc --noEmit` проходит |
| Миграция `0004` | ✅ Сгенерирована, проверена, **применена** |
| Фактическая БД | ✅ На `0004`, проверена read-only |
| Структура `features/` | ✅ Переезд выполнен (`858dc45`) |
| `lib/action-client.ts` | ✅ Создан |
| Zod установлен | ✅ `zod@^4` прямой зависимостью |
| `server-only` установлен | ✅ |
| `src/types/index.ts` удалён | ✅ |
| `src/hooks/test.ts` удалён | ✅ |
| Справочники из БД вместо хардкода | ✅ Этап 0.4 |
| Сид `cities` / `categories` | ✅ Написан и выполнен |
| CI | ✅ Этап 0.5 |

**Этап 0 закрыт полностью.** Следующий — 1.1, вертикальный срез услуг.

### Миграция `0004` — завершена

**`drizzle/0004_white_miek.sql` применена к базе.** Сгенерирована `drizzle-kit generate`,
зарегистрирована в журнале (`idx: 4`), снимок `drizzle/meta/0004_snapshot.json` создан.
`drizzle.__drizzle_migrations` содержит 5 записей — миграция засчитана.

Временный reference-файл `0004_v1_catalog.sql` своё назначение выполнил и удалён.

#### Фактическое состояние БД проверено read-only

После применения выполнена проверка живой базы. Все запросы читающие — `SELECT` по
таблицам и системному каталогу, без `INSERT` / `UPDATE` / `DELETE` и DDL.
**Пройдены все 10 проверок, расхождений с моделью нет:**

| # | Проверка | Результат |
|---|---|---|
| 1 | `drizzle.__drizzle_migrations` содержит `0004` | 5 записей ✅ |
| 2 | Созданы `profile_contacts`, `service_images`, `favorites` | все три ✅ |
| 3 | Удалены `responses`, `reviews`, `profiles.phone`, `services.image` | ничего не найдено ✅ |
| 4 | 5 enum-типов с верными значениями и порядком | 5 из 5 ✅ |
| 5 | `cities.slug` — `NOT NULL`, `UNIQUE`, 8 верных слагов | 0 пустых, все уникальны ✅ |
| 6 | `tasks.status` — `task_status`, `NOT NULL`, default `'open'` | ✅ |
| 7 | `services` и `tasks` пусты | 0 / 0 ✅ |
| 8 | `profiles` — 3 строки | 3 ✅ |
| 9 | `categories` — 20 строк | 20 ✅ |
| 10 | 5 FK с `ON DELETE CASCADE`, 16 индексов, 3 CHECK | 5 / 16 / 3 ✅ |

**Данные сохранены полностью:** 3 пользователя, 3 профиля, 20 категорий, 8 городов.
Ни одна строка не потеряна. Таблицы `responses` и `reviews` удалены согласно
утверждённому плану — обе были пусты, что подтверждено до применения.

Два частичных уникальных индекса `favorites` создались с предикатом `WHERE`, как
и задумано. CHECK-ограничения PostgreSQL нормализовал, убрав квалификацию имён колонок —
это ожидаемое поведение, смысл выражений не изменился.

### Этап 0.4 — справочники из БД

Города и категории больше не захардкожены в компонентах: одиннадцать компонентов
и страниц получают их из базы. Источник истины — таблицы `cities` и `categories`.

**Модули запросов:** `features/cities/queries.ts` и `features/categories/queries.ts` —
отдельные фичи, а не общий `reference/`. Правило «фича = сущность БД» осталось без
исключений, и когда появится управление категориями из админки, `actions.ts` ляжет
в готовое место. Оба начинаются с `import "server-only"` и селектят только нужные
колонки.

React `cache()` намеренно не используется: сейчас справочник запрашивается один раз
на рендер, мемоизировать нечего. Условие, при котором обёртку стоит ввести, записано
комментарием в обоих файлах. Изменение обратное и не затрагивает вызывающий код.

**Иконки и цвета категорий.** В БД лежит только имя иконки lucide. Карты
`slug → компонент` и `slug → цвет` живут в `lib/constants.ts`: цвет относится
к дизайн-системе, а не к данным, и Tailwind-классы в базе не работают — CSS
собирается сканированием исходников. Карта иконок явная, а не `import * as Icons`
с обращением по ключу: динамическое обращение ломает tree-shaking.

Ограничение известно и записано в коде: категория, добавленная в обход карты,
отрисуется нейтральным цветом. Переход на токен в колонке `categories.color`
имеет смысл, когда появится управление категориями из админки.

**Сид.** `src/lib/db/seed.ts`, команда `npm run db:seed`, раннер `tsx`.
Идемпотентный: `INSERT ... ON CONFLICT (slug) DO UPDATE` — вставляет отсутствующее,
обновляет имя, иконку и порядок у существующего. Ничего не удаляет и не меняет
идентификаторы: на них уже могут ссылаться объявления.

Выполнен и проверен read-only. Результат: 8 городов с `id` 1–8 и 20 категорий
с `id` 1–20 — идентификаторы не изменились, лишних и удалённых записей нет.
Единственное изменение данных: у городов проставлен `order` 1–8 вместо 0.

**Побочные правки, вызванные переходом:** значения селектов сменились с названий
на идентификаторы (иначе Server Action не сможет записать `cityId` и `categoryId`);
`services/[slug]/page.tsx` конвертирован в серверный компонент — директива
`"use client"` там стояла без причины; `CategoryCard.count` стал необязательным,
поскольку реальный счётчик объявлений появится на этапе 1; `StatusFilter` переведён
на `TASK_STATUSES` и лишился статуса `in_progress`; из админки удалена колонка «роль»
вместе с константой `USER_ROLES`.

### Этап 0.5 — CI

`.github/workflows/ci.yml`, четыре шага: `tsc --noEmit`, `biome lint`,
`biome ci --changed`, `next build`. Все проходят.

**Форматирование проверяется только у изменённых файлов.** Легаси написано до появления
конфига Biome и расходится с ним примерно в 130 местах — по кавычкам, точкам с запятой
и порядку импортов. Разовое форматирование всего репозитория отложено сознательно,
поэтому стандарт применяется к тому, что реально правят: база сходится к единому стилю
по мере редактирования. Пятнадцать файлов, затронутых этапом 0.4, приведены к стандарту.

Шаг с `--changed` выполняется только на pull request: при нуле изменённых файлов
`biome ci --changed` завершается с кодом 1. В `checkout` нужен `fetch-depth: 0`,
иначе git не найдёт базовую ветку для сравнения.

**Сборка запускается как `npx next build`, а не `npm run build`** — скрипт сборки
начинается с `drizzle-kit migrate`, и на CI это применяло бы миграции к базе при каждом
прогоне. Это же стоит учесть при настройке деплоя.

Из области проверки Biome исключены генерируемые файлы: `drizzle/meta`, `package.json`,
`package-lock.json`, `components.json`. Форматировать их бессмысленно — перепишет
следующая генерация. `components/ui/` **не исключён**: это код проекта, просто созданный
из шаблона.

**`.gitattributes` фиксирует LF** во всех окружениях. Без него на Windows при
`core.autocrlf=true` рабочее дерево получало CRLF, а Biome ожидает LF — локальная
проверка показывала на 42 ошибки больше, чем CI на Linux. Расхождение между машиной
разработчика и сборкой — источник споров вида «у меня всё зелёное».

**Линтер как таковой чист.** Из ~130 замечаний ни одно не относится к правилам качества
кода: всё это форматирование и порядок импортов. `biome lint` по всему проекту даёт
0 ошибок и 18 предупреждений, 13 из которых — в сгенерированных `shadcn` файлах.

#### Замечание для слоя запросов

Драйвер Neon возвращает `bigint` **строкой**, а не числом. В `schema.ts` колонки
объявлены с `mode: 'number'`, поэтому Drizzle преобразует их сам — но при сырых запросах
в обход ORM строгое сравнение идентификаторов даст ложное несовпадение. Учитывать при
написании `queries.ts`.

#### Ручные правки в миграции

Автогенерация выдала три нерабочих оператора, все исправлены и помечены комментариями
прямо в файле:

| Место | Что было не так |
|---|---|
| `cities.slug` | `ADD COLUMN ... NOT NULL` падает на восьми существующих городах. Заменено на добавление nullable → 8 `UPDATE` → `SET NOT NULL` |
| `tasks.status`, default | `SET DEFAULT 'open'::task_status` выполнялся до смены типа, когда колонка ещё `integer` |
| `tasks.status`, приведение | `USING "status"::task_status` — приведения `integer → enum` в PostgreSQL не существует. Заменено на `CASE` |

Правка SQL не нарушает снимок: снимок описывает целевое состояние схемы, а не
последовательность шагов, поэтому будущие `db:generate` продолжат работать корректно.

#### Ограничение среды

`db:generate` невозможно выполнить неинтерактивно: drizzle-kit спрашивает, не являются
ли создаваемые таблицы переименованием удаляемых, и требует TTY. Команду нужно
запускать в обычном терминале, отвечая **create / add**, а не rename.

---

### Этап 1.1 — услуги, вертикальный срез

Первая фича на реальных данных. Заданный ею шаблон повторяется в заданиях,
профиле и избранном.

**Модули:** `features/services/{schemas,queries,actions}.ts`, компоненты формы
и переключателя видимости, `lib/format.ts` для цен и дат.

**Условие видимости вынесено в одну константу** внутри `queries.ts`:

```ts
const isPubliclyVisible = and(
  eq(services.isActive, true),
  eq(services.moderationStatus, "approved"),
);
```

Так его нельзя забыть в новом запросе, а забыть — значит показать в каталоге то,
что владелец выключил или модератор заблокировал.

**Контактов нет ни в одном запросе услуг.** Раскрытие сделано отдельным серверным
действием после проверки сессии: попади контакты в выборку детальной страницы,
они оказались бы в HTML и читались бы из исходного кода без нажатия кнопки.
Реализовано раньше плана — детальная страница иначе осталась бы с заглушкой.

**Адрес объявления канонический:** `/services/[categorySlug]/[id]`. Если slug
в адресе не совпадает с категорией объявления, страница отдаёт 404, а не показывает
содержимое по чужому пути.

**Чужое объявление в форме правки отдаёт `notFound`, а не 403** — 403 подтвердил бы,
что объявление с таким идентификатором существует.

#### Ошибка, которую нашла только ручная проверка

Первая отправка формы отказала с общим сообщением и без единой подсказки под полями.
Причина: в zod 4 включение `z.undefined()` в union **не делает ключ необязательным** —
нужен `.optional()`. Невыбранный чекбокс в `FormData` отсутствует вовсе, поэтому форма
ломалась ровно тогда, когда «Договорная» не отмечена, то есть в обычном случае.

Ни `tsc`, ни линтер, ни сборка это не видят: ошибка в соответствии между разметкой
формы и схемой валидации. Отсюда два вывода, зафиксированных на будущее:

1. К концу этапа 1 нужны тесты на схемы. Семь случаев занимают тридцать строк
   и ловят весь этот класс ошибок.
2. Поля без собственной подсказки под инпутом должны показывать ошибку хотя бы
   в общей плашке — иначе форма отказывает молча. Добавлено в `CreateServiceForm`.

#### Не сделано в 1.1

**Фильтры каталога.** В дорожной карте значится «список с фильтрами», но
`getServiceCardsByCategory` принимает только slug категории. Сайдбар с фильтрами
по городу и типу исполнителя остался на локальном `useState` и ни на что не влияет.
Правильный способ — через `searchParams`: фильтр меняет адрес, серверный компонент
читает параметры. Тогда состояние остаётся в URL и работает кнопка «назад».

**Пагинация** убрана со страницы каталога: разметка была статичной и вела в никуда.
По дорожной карте пагинация относится к этапу 3.

---

## Известные проблемы

**Требуют решения до соответствующего этапа:**

1. ~~Схема БД не соответствует модели v1~~ — **закрыто миграцией `0004`.**
2. ~~Раскрытие контактов небезопасно по конструкции~~ — **закрыто на 1.1:** контакты
   не приходят пропсами и отсутствуют в разметке, запрашиваются серверным действием
   после проверки сессии. Заполнять их пока негде — форма контактов на этапе 1.3.
3. ~~Нет индексов на доменных таблицах~~ — **закрыто:** 16 индексов созданы.
4. ~~Дублирование справочников~~ — **закрыто этапом 0.4:** города и категории
   читаются из БД, содержимое зафиксировано сидом.
5. ~~`userId` nullable в `services` и `tasks`~~ — **закрыто:** обе колонки `NOT NULL`.
6. **Вёрстка шире модели v1 — частично** — остались отклики и срок задания на
   страницах заданий, отзывы на профиле. Список — в DATA-MODEL.md.
7. **Критерий «Проверенный» не зафиксирован** — в отсутствие отзывов это основной
   сигнал доверия, произвольная выдача обесценит его. DATA-MODEL.md, вопрос 1.
8. **Фильтры каталога не работают** — сайдбар меняет локальное состояние и ни на что
   не влияет. Нужен переход на `searchParams`. Осталось от 1.1.
9. **Нет тестов на схемы валидации** — класс ошибок, который не ловят ни типы,
   ни сборка. Одна такая уже была, см. раздел про этап 1.1.

**Баги в UI:**

10. ~~Опечатка «Слободея»~~ — **закрыто на 0.4**
11. ~~`ServiceCard` ведёт на несуществующий `/services/listing/{id}`~~ —
    **закрыто на 1.1:** адрес стал `/services/[categorySlug]/[id]`
12. Ссылка на профиль `/profile/{userId}` — неверны и путь, и параметр
    (нужен `/profiles/{username}`) — `tasks/[id]/page.tsx:228`. Закроется на 1.2
    вместе с переписыванием страницы задания.

**Процессные:**

13. Работа велась напрямую в `main`, без веток и PR. Часть сообщений коммитов
    неинформативна (`asdf`, `dddf`, `11d`). Для коммерческого продукта стоит перейти
    на ветки + PR, особенно при появлении второго разработчика.
