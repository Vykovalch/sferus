/**
 * Статусы задания. Значения совпадают с pg-enum `task_status`.
 * `in_progress` в v1 нет намеренно: он подразумевает известного исполнителя,
 * а связи задания с исполнителем без откликов не существует.
 */
export const TASK_STATUSES = {
  open: "Открыто",
  completed: "Завершено",
  cancelled: "Отменено",
} as const;

export type TaskStatus = keyof typeof TASK_STATUSES;

/** Значение фильтра «все города» / «все категории» — не выбранный вариант. */
export const ALL_OPTION = "all";

/**
 * Высота шапки на десктопе в пикселях — держать синхронно с `lg:h-[72px]`
 * в `Header.tsx`.
 * Используется как компенсация `rootMargin` в IntersectionObserver, который
 * следит за секцией Hero: без неё триггер срабатывал бы по геометрии
 * вьюпорта, а не по факту перекрытия sticky-шапкой. От результата зависит
 * и поле поиска в шапке (от `xl`), и лупа на узких экранах.
 *
 * На узких экранах шапка ниже — `h-16`, 64px, — и секция считается скрытой,
 * когда её последние ~8px ещё видны под шапкой. Одна константа на все ширины —
 * решение владельца (2026-09-19): низ секции Hero — фон без элементов.
 */
export const HEADER_HEIGHT_PX = 72;
